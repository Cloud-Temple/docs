# Exemple de configuration pour le script de traduction (scripts/translate.js)

# --- Configuration de l'API Cloud Temple LLMaaS ---

# URL de l'API Cloud Temple LLMaaS.
# La valeur par défaut dans le script est 'https://api.ai.cloud-temple.com/v1/chat/completions'
# Décommentez et modifiez si vous utilisez une URL différente (ex: instance locale, autre endpoint).
# CLOUDTEMPLE_API_URL=https://api.ai.cloud-temple.com/v1/chat/completions

# Votre clé API pour accéder au service Cloud Temple LLMaaS.
# REMPLACEZ CECI PAR VOTRE VRAIE CLE API.
CLOUDTEMPLE_API_KEY="VOTRE_CLE_API_CLOUDTEMPLE_LLMAAS_ICI"

# --- Configuration du Modèle de Traduction ---

# Modèle à utiliser pour la traduction.
# La valeur par défaut dans le script est 'Qwen/Qwen3-30B-A3B-FP8'.
# Décommentez et modifiez pour utiliser un autre modèle compatible.
# TRANSLATION_MODEL="Qwen/Qwen3-30B-A3B-FP8"

# Température pour la génération de la traduction (nombre entre 0 et 2).
# Des valeurs plus élevées (ex: 0.8) rendent la sortie plus aléatoire,
# tandis que des valeurs plus basses (ex: 0.2) la rendent plus focalisée et déterministe.
# La valeur par défaut dans le script est 1.
# TRANSLATION_TEMPERATURE=0.7

# Alternative au sampling avec température, appelée nucleus sampling.
# Le modèle considère les résultats des tokens avec une masse de probabilité top_p.
# Ainsi, 0.1 signifie que seuls les tokens composant les 10% de masse de probabilité supérieure sont considérés.
# La valeur par défaut dans le script est 1.
# TRANSLATION_TOP_P=1

# --- Configuration du Script de Traduction ---

# Chemin de base vers le dossier racine de la documentation Docusaurus.
# Utile si le script n'est pas exécuté depuis la racine du projet Docusaurus.
# La valeur par défaut dans le script est '.' (répertoire courant).
# DOC_BASE_PATH=.

# Nombre de fichiers à traiter en parallèle par le script.
# Une valeur plus élevée peut accélérer la traduction mais peut aussi atteindre les limites de débit de l'API.
# La valeur par défaut dans le script est 4.
# CONCURRENT_TRANSLATIONS=2
